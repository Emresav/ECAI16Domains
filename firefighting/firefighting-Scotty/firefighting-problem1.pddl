(define (problem firefighting-problem1)
	(:domain firefighting1)

	(:init 
        (fire1-on)
        ; (fire1-extinguished)
        (x 0 0)
        (y 0 0)
        ; (x 200 200)
        ; (y -100 -100)

    )

	(:goal
		(and
            (photo1)
            ))

)