(define (problem auv-3D-problem1)
	(:domain auv-3D)

	(:init 
        (can-move)
        (x 0 0)
        (y 0 0)
        (depth 0 0)

    )

	(:goal
		(and
            (sample-taken)
            
            ))

)